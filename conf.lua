function love.conf(t)
    t.window.fullscreen = true -- Включить полноэкранный режим
    -- t.window.fullscreenType = "desktop" -- (по умолчанию) полноэкранный режим с разрешением рабочего стола
    -- Можно задать "exclusive" — полный эксклюзивный режим (иногда полезно для производительности)
end
